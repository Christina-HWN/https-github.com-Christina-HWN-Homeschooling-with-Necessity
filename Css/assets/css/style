/* Reset Default Browser Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Global Styles */
body {
    font-family: Arial, sans-serif;
    background-color: #f0f4f8;
    color: #333;
    line-height: 1.6;
}

/* Header */
header {
    background-color: #4CAF50;
    color: white;
    padding: 20px;
    text-align: center;
}

/* Hero Banner */
.hero {
    text-align: center;
    padding: 50px 20px;
    background-image: url('images/HWNBannerimage.webp'); /* Your banner image */
    background-size: cover;
    background-position: center;
    color: white;
    min-height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
}

@media (max-width: 768px) {
    .hero {
        background-size: contain;
        background-repeat: no-repeat;
        min-height: 250px;
    }
}

@media (max-width: 480px) {
    .hero {
        min-height: 200px;
        padding: 30px 10px;
    }
}

/* Navigation Bar */
nav {
    display: flex;
    justify-content: center;
    background-color: #333;
    padding: 10px 0;
    position: sticky;
    top: 0;
    z-index: 1000;
}

/* Navigation Links */
nav a {
    color: white;
    padding: 14px 20px;
    text-decoration: none;
    text-align: center;
}

nav a:hover {
    background-color: #ddd;
    color: black;
}

nav a.active {
    background-color: #4CAF50;
    color: white;
}

/* Content Section */
.content {
    padding: 20px;
    max-width: 1200px;
    margin: auto;
}

/* Content Titles */
.content h2 {
    text-align: center;
    color: #4CAF50;
}

/* Content Paragraphs */
.content p {
    line-height: 1.6;
}

/* Features Section (Container for Feature Boxes) */
.features {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
    margin-top: 30px;
}

/* Individual Feature Boxes */
.feature {
    flex: 1 1 300px;
    background-color: #ffffff;
    padding: 20px;
    border: 1px solid #ddd;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    text-align: center;
}

/* Feature Headings */
.feature h3 {
    color: #4CAF50;
}

/* Make All Images Responsive */
img {
    max-width: 100%;
    height: auto;
    display: block;
    margin: 0 auto;
}

/* Footer */
footer {
    text-align: center;
    padding: 20px;
    background-color: #333;
    color: white;
    margin-top: 20px;
}

/* New Styles from Inline */
ul.custom-list {
    list-style-type: none;
    padding: 0;
}

ul.custom-list li {
    margin-bottom: 20px;
}

ul.custom-list h3 {
    color: #4CAF50;
}
